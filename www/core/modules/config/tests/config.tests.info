[ConfigurationTest]
name = Configuration basic tests
description = Tests writing and reading configuration files.
group = Configuration
file = config.test

[ConfigurationHooksTest]
name = Configuration hooks
description = Tests hooks for validation, creating, updating, and deleting configurations.
group = Configuration
file = config.test

[ConfigurationUITest]
name = Configuration UI
description = Tests the UI for syncing, diffing, importing, and exporting configuration.
group = Configuration
file = config.test

[ConfigurationInstallDefaultHooksTest]
name = Configuration default config tests
description = Tests that hook_config_create() is run during installation of default config.
group = Configuration
file = config.test

; Added by Backdrop CMS packaging script on 2025-10-06
project = backdrop
version = 1.32.1
timestamp = 1759813356
