#ddev-generated
# If you remove the ddev-generated line above you
# are responsible for maintaining this file. DDEV will not then
# update it, for example if you add `additional_hostnames`, etc.

http:
  routers:
    borg-web-80-http:
      entrypoints:
        - http-80
      rule: HostRegexp(`^borg\.ddev\.site$`)
      
      service: "borg-web-80"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "borg-redirectHttps"
    borg-web-8025-http:
      entrypoints:
        - http-8025
      rule: HostRegexp(`^borg\.ddev\.site$`)
      
      service: "borg-web-8025"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "borg-redirectHttps"
    borg-xhgui-80-http:
      entrypoints:
        - http-8143
      rule: HostRegexp(`^borg\.ddev\.site$`)
      
      service: "borg-xhgui-80"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "borg-redirectHttps"
    
    
    
    borg-web-80-https:
      entrypoints:
        - http-443
      rule: HostRegexp(`^borg\.ddev\.site$`)
      
      service: "borg-web-80"
      ruleSyntax: v3
      
      tls: true
      
    borg-web-8025-https:
      entrypoints:
        - http-8026
      rule: HostRegexp(`^borg\.ddev\.site$`)
      
      service: "borg-web-8025"
      ruleSyntax: v3
      
      tls: true
      
    
    borg-xhgui-80-https:
      entrypoints:
        - http-8142
      rule: HostRegexp(`^borg\.ddev\.site$`)
      
      service: "borg-xhgui-80"
      ruleSyntax: v3
      
      tls: true
      
    

  middlewares:
    borg-redirectHttps:
      redirectScheme:
        scheme: https
        permanent: true

  services:
    borg-web-80:
      loadbalancer:
        servers:
          - url: http://ddev-borg-web:80
        
    borg-web-8025:
      loadbalancer:
        servers:
          - url: http://ddev-borg-web:8025
        
    
    
    borg-xhgui-80:
      loadbalancer:
        servers:
          - url: http://ddev-borg-xhgui:80
        
    
    

tls:
  certificates:
    - certFile: /mnt/ddev-global-cache/traefik/certs/borg.crt
      keyFile: /mnt/ddev-global-cache/traefik/certs/borg.key